# Django API base — baked in at BUILD time (set before `npm run build`).
# Same-origin / dev-proxy default:
VITE_API_BASE_URL=/api/v1
# Cross-origin deploy (e.g. folani.osdl.ir → api.folani.osdl.ir):
VITE_DEV_PROXY_TARGET=http://api.soleimani.osdl.ir